<%= form_for @meerkat_stream do |f| %>

    <% if @meerkat_stream.errors.any? %>
        <div id="error_explanation">
          <h2>
            <%= pluralize(@meerkat_stream.errors.count, "error") %> prohibited
            this stream from being saved:
          </h2>
          <ul>
            <% @meerkat_stream.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
          </ul>
        </div>
    <% end %>

    <p>
      <%= f.label :link %><br>
      <%= f.text_field :link %>
    </p>

    <p>
      <%= f.submit %>
    </p>

<% end %>